{
  "version": "{{version}}",
  "themeCollection": {
    "baseTheme": {
      "name": "{{theme_name}}",
      "version": "{{theme_version}}",
      "type": "{{theme_type}}"
    }
  },
  "activeSectionIndex": "{{active_section_index}}",
  "defaultDrillFilterOtherVisuals": "{{default_drill_filter}}",
  "slowDataSourceSettings": {
    "isCrossHighlightingDisabled": "{{is_cross_highlighting_disabled}}",
    "isSlicerSelectionsButtonEnabled": "{{is_slicer_selections_enabled}}",
    "isFilterSelectionsButtonEnabled": "{{is_filter_selections_enabled}}",
    "isFieldWellButtonEnabled": "{{is_field_well_enabled}}",
    "isApplyAllButtonEnabled": "{{is_apply_all_enabled}}"
  },
  "settings": {
    "useNewFilterPaneExperience": "{{use_new_filter_pane}}",
    "allowChangeFilterTypes": "{{allow_change_filter_types}}"
  }
}
