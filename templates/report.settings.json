{
  "version": "{{version}}",
  "settings": {
    "visualSettings": {
      "defaultVisualStyle": "{{default_visual_style}}",
      "showDataLabels": "{{show_data_labels}}"
    },
    "filterSettings": {
      "persistentFilters": "{{persistent_filters}}",
      "filterPaneEnabled": "{{filter_pane_enabled}}"
    },
    "interactionSettings": {
      "drillEnabled": "{{drill_enabled}}",
      "crossFilteringEnabled": "{{cross_filtering_enabled}}"
    }
  }
}
